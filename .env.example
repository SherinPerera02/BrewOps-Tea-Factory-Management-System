# Database
DB_HOST=localhost
DB_USER=root
DB_PASS=yourpassword
DB_NAME=brewops
DB_PORT=3306

# JWT
JWT_SECRET=your_jwt_secret
JWT_EXPIRES_IN=7d

# Mail (SMTP)
MAIL_HOST=smtp.example.com
MAIL_PORT=587
MAIL_USER=you@example.com
MAIL_PASS=yourpassword

# Optional SendGrid API Key
SENDGRID_API_KEY=

# Server
PORT=5000

# -----------------------------
# Example frontend/backend and production-ready settings
# NOTE: Do NOT commit real secrets. Use this file as a template and
# copy values into a local `.env` which is gitignored.
# -----------------------------

# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=softora_app
DB_PORT=3306

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-2024

# Server Configuration (Backend)
PORT=5000

# Frontend Configuration (for React app)
# This is an example value. In a CRA app place this in the front-end project's .env
REACT_APP_BACKEND_URL=http://localhost:5000

# Mail/Nodemailer Configuration
# Use a Gmail App Password or your SMTP provider credentials
MAIL_SERVICE=gmail
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USER=smperera574@gmail.com
# MAIL_PASS is intentionally left as a placeholder here. Replace in your local .env
MAIL_PASS=<REPLACE_WITH_SMTP_PASSWORD>
MAIL_FROM="BrewOps <smperera574@gmail.com>"
